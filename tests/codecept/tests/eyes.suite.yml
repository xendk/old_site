class_name: EyesTester
modules:
    enabled: 
      - EyesHelper
      - WebDriver
      - Argus
    config:
      WebDriver:
        url: 'http://test.xen.dev' # proctor:host
        browser: firefox
        window_size: 1280x1024
        # browser: phantomjs
        # window_size: 1280x1024
